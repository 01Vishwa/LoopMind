## pdf specifics
Use `pypdf` if available, else report that text extraction is unavailable in
Notes. Report page count as row_count and up to 500 chars of extracted text per
page for at most 3 pages under Notes.
