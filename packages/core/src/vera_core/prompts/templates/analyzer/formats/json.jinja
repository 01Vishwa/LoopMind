## json specifics
`json.load` the file. Detect the root type: if it is a list of objects, treat
each key as a field and show 3 sample items; if it is a dict, list its top-level
keys and their value types. Note nesting depth in Notes.
