## sqlite specifics
Use `sqlite3`. List every table in the sheet_names line. For each table emit its
columns and declared types in the Fields block prefixed with the table name, and
its row count in Notes.
