fastapi>=0.110.1
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.4
pandas>=2.2.3
openpyxl>=3.1.2
pdfplumber>=0.11.0
Markdown>=3.6
supabase>=2.4.0
python-dotenv>=1.0.0
tenacity>=8.3.0
sse-starlette>=2.1.0

# NVIDIA NIM (replaces google-generativeai)
langchain-nvidia-ai-endpoints>=0.3.0
langchain-core>=0.3.0

# Artifact output pipeline
# (standard library only — no extra dep needed for base64/os)

# Docker sandbox executor (deferred — requires Docker Desktop)
# docker>=7.0.0

# Redis rate limiting (deferred — requires Redis)
# slowapi>=0.1.9
# redis>=5.0.0

# Celery SSE resilience (deferred — requires Redis)
# celery[redis]>=5.4.0

# Test suite
pytest>=8.0.0
pytest-asyncio>=0.23.0
