fastapi>=0.110.1
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.4
pandas>=2.2.3
pyarrow>=16.0.0
openpyxl>=3.1.2
pypdf>=4.0.0
pdfplumber>=0.11.0
Markdown>=3.6
supabase>=2.4.0
PyJWT>=2.8.0
python-dotenv>=1.0.0
tenacity>=8.3.0
sse-starlette>=2.1.0

# NVIDIA NIM (replaces google-generativeai)
langchain-nvidia-ai-endpoints>=0.3.0
langchain-core>=0.3.0

# Artifact output pipeline
# (standard library only — no extra dep needed for base64/os)

# Docker sandbox executor (activate with DOCKER_SANDBOX_ENABLED=true)
docker>=7.0.0

# File magic-byte validation (MIN-04) — bundles libmagic for Windows
python-magic-bin>=0.4.14

# Embedding model for retrieval (PERF-01, PERF-02)
sentence-transformers>=2.7.0

# Test suite
pytest>=8.0.0
pytest-asyncio>=0.23.0
